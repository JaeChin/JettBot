# STT - Speech to Text
faster-whisper==1.0.3
ctranslate2==4.4.0

# CUDA support (Windows)
nvidia-cublas-cu12
nvidia-cudnn-cu12

# VAD - Voice Activity Detection (Silero VAD via torch.hub)
torchaudio

# Audio capture and processing
sounddevice==0.4.6
soundfile==0.12.1
numpy>=1.26.0,<2.0.0

# HTTP (required by faster-whisper but not declared)
requests

# Cloud LLM (hybrid routing)
anthropic>=0.40.0

# Utilities
python-dotenv==1.0.1

# Testing
pytest>=8.0.0
