# Claude API (for hybrid routing — used by Anthropic SDK)
ANTHROPIC_API_KEY=your-api-key-here

# PostgreSQL (VPS)
POSTGRES_USER=jett
POSTGRES_PASSWORD=generate-strong-password
POSTGRES_DB=jett

# n8n (VPS)
N8N_USER=admin
N8N_PASSWORD=generate-strong-password

# Portainer (VPS)
PORTAINER_TOKEN=your-portainer-token

# WireGuard
VPS_PUBLIC_IP=your-vps-ip
